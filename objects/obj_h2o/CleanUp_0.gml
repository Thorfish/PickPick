if (surface_exists(ca_surface_a)) {
    surface_free(ca_surface_a);
}
if (surface_exists(ca_surface_b)) {
    surface_free(ca_surface_b);
}
if (surface_exists(render_surface)) {
    surface_free(render_surface);
}